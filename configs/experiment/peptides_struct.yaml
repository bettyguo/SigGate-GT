# Experiment config: peptides_struct
# Full training run matching paper hyperparameters

defaults:
  - /config
  - _self_

task: peptides_struct

model:
  _target_: siggate_gt.models.SigGateGT
  out_dim: 11

data:
  _target_: null

# Override training for this benchmark
training:
  epochs: 200
  optimizer:
    lr: 5.0e-4
    weight_decay: 1.0e-5
